import { Logger } from '../../models/logger';
import { GridComponentProps } from '../../GridComponentProps';
import { GridApiRef } from '../../models/api/gridApiRef';
export declare function useGridLoggerFactory(apiRef: GridApiRef, props: Pick<GridComponentProps, 'logger' | 'logLevel'>): void;
export declare function useGridLogger(apiRef: GridApiRef, name: string): Logger;
