import { GridApiRef } from '../../../models/api/gridApiRef';
import { GridComponentProps } from '../../../GridComponentProps';
/**
 * @requires useGridContainerProps (state)
 * @requires useGridColumns (state)
 * @requires useGridPage (state)
 * @requires useGridPageSize (state)
 * @requires useGridRows (state)
 * @requires useGridDensity (state)
 */
export declare const useGridVirtualization: (apiRef: GridApiRef, props: Pick<GridComponentProps, 'pagination' | 'paginationMode' | 'columnBuffer' | 'disableExtendRowFullWidth' | 'disableVirtualization'>) => void;
